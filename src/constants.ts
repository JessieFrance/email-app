// Total number of messages.
// In the future, this could be determined by loading
// from the SQL database.
export const NUMBER_MESSAGES = 10;

// Email frequency in seconds.
export const EMAIL_FREQUENCY = 60;
